hotkey: "ctrl+`"
whisper_model: "small.en"
device: "cpu"
sample_rate: 16000
channels: 1
mic_device: null        # null = system default; name or index to select a specific mic
inject_method: clipboard  # "clipboard" (fast Ctrl+V) or "typewrite" (per-key typing)
beep_enabled: true
ai_cleanup:
  enabled: false
  provider: "ollama"
  model: "llama3.2:3b"
  api_key: ""
  endpoint: "http://localhost:11434/api/generate"
